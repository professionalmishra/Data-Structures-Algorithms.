#here ill provide only the method and logic
#day 4 of DSA
// Method for Searching a Element in a linked List
    public static int searchInLinkedList(Node head, int k)
    {
        Node temp = head;
        while (temp != null) {
            if (temp.data == k) {
                return 1;
            }
            temp = temp.next;  // Move to the next node
        }
        return 0;
    }


// Method to count the number of nodes in the list
public int countNodes() {
    int count = 0;
    Node current = head;
    while (current != null) {
        count++;
        current = current.next;
    }
    return count;
}
